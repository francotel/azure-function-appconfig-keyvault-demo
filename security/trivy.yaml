# -----------------------------------
# 🔍 Trivy configuration
# -----------------------------------

severity:
  - HIGH
  - CRITICAL

exit-code: 1

ignore-unfixed: true

scan:
  scanners:
    - vuln
    - misconfig
    - secret
  skip-dirs:
    - .terraform
    - node_modules

pkg:
  type:
    - library

format: table